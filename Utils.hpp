#ifndef UTILS_H
#define UTILS_H

#include <iostream>
#include <string>

#include <cerrno>
#include <cstring>

int		ft_stoi(const std::string& str);
void	printError(const char* msg);
#endif // UTILS_H
